<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Join Chat Room</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@700;400&family=Roboto:wght@400;500&display=swap');
    body {
      font-family: 'Montserrat', 'Roboto', Arial, sans-serif;
      background: linear-gradient(120deg, #ffecd2 0%, #fcb69f 100%);
      min-height: 100vh;
      margin: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      overflow: hidden;
    }
    /* Animated gradient overlay */
    body::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: linear-gradient(120deg, #667eea 0%, #764ba2 40%, #ff6a88 80%, #ffd6e0 100%);
      background-size: 200% 200%;
      z-index: 0;
      animation: gradientMove 18s ease-in-out infinite;
      opacity: 0.85;
    }
    @keyframes gradientMove {
      0% {background-position: 0% 50%;}
      50% {background-position: 100% 50%;}
      100% {background-position: 0% 50%;}
    }
    .container {
      position: relative;
      z-index: 1;
      background: rgba(255, 255, 255, 0.13);
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.18);
      border-radius: 24px;
      padding: 40px 36px 32px 36px;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-width: 340px;
      max-width: 95vw;
      backdrop-filter: blur(8px);
      border: 1.5px solid rgba(255,255,255,0.18);
    }
    h2 {
      font-family: 'Montserrat', sans-serif;
      font-size: 2.1rem;
      font-weight: 700;
      margin-bottom: 28px;
      color: #fff;
      text-shadow: 0 2px 16px rgba(118,75,162,0.18), 0 1px 0 #764ba2;
      letter-spacing: 1px;
      text-align: center;
      line-height: 1.3;
    }
    form {
      display: flex;
      flex-direction: column;
      gap: 18px;
      width: 100%;
      align-items: center;
    }
    input[type="text"] {
      padding: 14px 16px;
      border: none;
      border-radius: 8px;
      width: 100%;
      max-width: 260px;
      font-size: 1.1rem;
      font-family: 'Roboto', Arial, sans-serif;
      background: rgba(255,255,255,0.85);
      color: #4b2067;
      box-shadow: 0 2px 8px rgba(118,75,162,0.10);
      outline: none;
      transition: box-shadow 0.2s, background 0.2s;
    }
    input[type="text"]:focus {
      box-shadow: 0 4px 16px rgba(102,126,234,0.15);
      background: #fff;
    }
    .btn {
      padding: 12px 28px;
      border: none;
      border-radius: 8px;
      font-size: 1.08rem;
      font-family: 'Montserrat', sans-serif;
      font-weight: 600;
      background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);
      color: #fff;
      cursor: pointer;
      box-shadow: 0 2px 8px rgba(118,75,162,0.10);
      transition: background 0.2s, transform 0.1s;
      margin-bottom: 8px;
      letter-spacing: 0.5px;
    }
    .btn:hover {
      background: linear-gradient(90deg, #ff6a88 0%, #ffb86c 100%);
      color: #fff;
      transform: translateY(-2px) scale(1.04);
    }
    .home-btn {
      margin-top: 10px;
      background: linear-gradient(90deg, #48bb78 0%, #38a169 100%);
      color: #fff;
    }
    .home-btn:hover {
      background: linear-gradient(90deg, #43e97b 0%, #38f9d7 100%);
      color: #4b2067;
    }
    @media (max-width: 480px) {
      .container {
        padding: 22px 8vw 20px 8vw;
        min-width: 0;
      }
      h2 {
        font-size: 1.2rem;
      }
      input[type="text"] {
        max-width: 98vw;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>
      <span style="font-size:1.2em; color:#ffd6e0; text-shadow:0 2px 8px #ff6a88;">👋</span>
      Welcome, <span style="color:#ffb86c; text-shadow:0 2px 8px #ff6a88;"><%= user.name %></span>!<br>
      <span style="font-size:1.05em; color:#764ba2;">Join a Chat Room</span>
    </h2>
    <form action="/chat/join" method="POST">
      <input type="text" name="room" required placeholder="Enter Room Name" autocomplete="off" />
      <button type="submit" class="btn">Join Room</button>
    </form>
    <button class="btn home-btn" onclick="window.location.replace('/home')">Go to Homepage</button>
  </div>
</body>
</html>